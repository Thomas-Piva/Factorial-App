<!-- Generated: 2026-04-05 | Files scanned: 62 | Token estimate: ~500 -->

# Data

## Database (Supabase / PostgreSQL)

### Tables & Key Columns

```
store
  id uuid PK | name | code | address | timezone

user
  id uuid PK (= auth.users.id)
  email | first_name | last_name | preferred_name
  role: UserRole = "employee" | "manager" | "admin"
  admission_date | is_active

store_membership
  id | user_id FK→user | store_id FK→store
  role: UserRole | is_primary bool | joined_at

shift_template
  id | store_id FK→store | label | shift_type: ShiftType
  start_time | end_time | color | is_active

shift_assignment
  id | user_id FK→user | store_id FK→store | template_id FK→shift_template
  date | shift_type: ShiftType | label | color
  start_time | end_time | published_at | notes

notification
  id | user_id FK→user | created_by FK→user
  title | body | is_read | created_at
```

### Enums (defined in src/types/database.ts)

```
ShiftType = "work_shift" | "rest_day" | "holiday" | "transfer" | "permission"
UserRole  = "employee" | "manager" | "admin"
```

### FK Dependency Order
```
auth.users → auth.identities
auth.users → user
user + store → store_membership
store → shift_template
user + store + shift_template → shift_assignment
user → notification
```

## Generated Types
- `src/types/database.ts` — auto-generated by `supabase gen types` (348 lines)
- `src/types/domain.ts` — hand-written view types built on DB types:
  - `ShiftBlock` / `ShiftCell` / `ShiftGridRow` — grid rendering model
  - `UserWithInitials` — user + computed display fields
  - `WeekRange` / `MonthRange` — calendar navigation

## Local Dev
- `supabase/seed.sql` — 4 users, 1 store, memberships, templates, shifts, notifications
- `supabase/config.toml` — `enable_confirmations = false`
- Test user: `test@example.com` / `password123` (created via Admin API, not SQL seed)
